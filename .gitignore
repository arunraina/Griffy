node_modules/
dist/
.next/
.expo/
*.env
.env.*
!.env.example
*.env.local
.turbo/
coverage/
prisma/migrations/
test-results/
playwright-report/

# Local-disk fallback for server-side uploads (invoices etc.) when S3 isn't
# configured — never contains anything that should be committed.
apps/api/uploads/

# OS
.DS_Store
